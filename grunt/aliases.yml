default:
  - prompt:main
  - inputHandler

lint:
  - jshint

format:
  - exec:jscs

test:
  - lint
  - karma

build:
  - clean
  - test
  - copy
  - sass
  - browserify:app

staging:
  - build
  - connect:staging

dev:
  - build
  - concurrent:dev

pre-commit:
  - format
  - test
