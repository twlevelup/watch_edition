default:
  - prompt:main
  - inputHandler

lint:
  - jshint

format:
  - exec:jscs

test:
  - lint
  - karma

build:
  - copy
  - sass
  - webpack:build

staging:
  - build
  - connect:staging

dev:
  - clean
  - test
  - build
  - concurrent:dev

pre-commit:
  - format
  - test
