default:
  - prompt:dev
  - inputHandler

lint:
  - jshint

format:
  - exec:jscs

test:
  - lint
  - karma

build:
  - clean
  - test
  - copy
  - sass
  - browserify:app

staging:
  - build
  - connect:staging

dev:
  - build
  - concurrent

pre-commit:
  - format
  - test
