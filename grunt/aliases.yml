default:
  - prompt:dev
  - inputHandler

lint:
  - jshint

test:
  - lint
  - watchify:test
  - jasmine

build:
  - test
  - clean
  - copy
  - sass
  - watchify:app

staging:
  - build
  - connect:staging

dev:
  - build
  - connect:dev
  - watch
